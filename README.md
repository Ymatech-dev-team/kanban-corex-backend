# kanban-corex-backend

API do Sistema de Tasks (Fastify + Prisma + Neon). Pronta para Vercel serverless (`api/` + `vercel.json`) ou Node host (Render/Railway via `build` + `start`). Contratos compartilhados em `vendor/contracts`.
